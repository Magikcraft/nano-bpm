{
  "name": "@nanobpm/nano-app-schema",
  "version": "0.0.0",
  "private": true,
  "description": "Source of truth for the Urban App manifest (nano.app.json): the JSON Schema (ADR 0027) plus its generated TypeScript types. See README.md.",
  "type": "module",
  "scripts": {
    "gen": "node scripts/gen-types.mjs",
    "validate": "node scripts/validate.mjs",
    "test": "npm run validate && npm run gen -- --check"
  },
  "devDependencies": {
    "ajv": "^8.17.1",
    "json-schema-to-typescript": "^15.0.3"
  }
}
