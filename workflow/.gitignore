node_modules/

# dist/ is a committed build artifact (the tsc-emitted JS + .d.ts consumers
# import without re-bundling). Re-include it against the repo-root `dist/`
# ignore, which targets the wasm FFI tree, not this package.
!dist/
!dist/**

# local integration-test scratch (dedicated gateway data dirs + logs)
.it/

# gateway RAD scratch (created when the integration gateway boots)
nanobpm-workspace/
