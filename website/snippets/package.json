{
  "name": "nanobpm-website-snippets",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Compilable sources for the code snippets rendered on nanobpm.io. Type-checked in CI so the landing-page examples never drift from the real @nanobpm/* APIs.",
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "devDependencies": {
    "@nanobpm/workflow": "0.7.0",
    "typescript": "^5.6.0"
  }
}
