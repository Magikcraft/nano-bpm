## Endpoint definitions

paths:
  /agent-instances:
    post:
      x-eventually-consistent: false
      tags:
        - Agent instance
      operationId: createAgentInstance
      security:
        - bearerAuth: []
        - basicAuth: []
      summary: Create agent instance
      description: |
        Creates a new agent instance. The returned key identifies the instance and must
        be used in subsequent update and query calls.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AgentInstanceCreationRequest'
      responses:
        "200":
          description: The agent instance was created.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AgentInstanceCreationResult'
        "400":
          $ref: 'common-responses.yaml#/components/responses/InvalidData'
        "401":
          $ref: 'common-responses.yaml#/components/responses/Unauthorized'
        "403":
          $ref: 'common-responses.yaml#/components/responses/Forbidden'
        "404":
          description: |
            The elementInstanceKey does not correspond to an active element instance.
            More details are provided in the response body.
          content:
            application/problem+json:
              schema:
                $ref: 'problem-detail.yaml#/components/schemas/ProblemDetail'
        "500":
          $ref: 'common-responses.yaml#/components/responses/InternalServerError'
        "503":
          $ref: 'common-responses.yaml#/components/responses/ServiceUnavailable'
      x-added-in-version: "8.10"

  /agent-instances/{agentInstanceKey}:
    get:
      x-eventually-consistent: true
      tags:
        - Agent instance
      operationId: getAgentInstance
      security:
        - bearerAuth: []
        - basicAuth: []
      summary: Get agent instance
      description: Returns agent instance as JSON.
      parameters:
        - name: agentInstanceKey
          in: path
          required: true
          description: The key of the agent instance to retrieve.
          schema:
            $ref: 'keys.yaml#/components/schemas/AgentInstanceKey'
      responses:
        "200":
          description: The agent instance is successfully returned.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AgentInstanceResult'
        "400":
          $ref: 'common-responses.yaml#/components/responses/InvalidData'
        "401":
          $ref: 'common-responses.yaml#/components/responses/Unauthorized'
        "403":
          $ref: 'common-responses.yaml#/components/responses/Forbidden'
        "404":
          description: |
            The agent instance with the given key was not found.
            More details are provided in the response body.
          content:
            application/problem+json:
              schema:
                $ref: 'problem-detail.yaml#/components/schemas/ProblemDetail'
        "500":
          $ref: 'common-responses.yaml#/components/responses/InternalServerError'
        "503":
          $ref: 'common-responses.yaml#/components/responses/ServiceUnavailable'
      x-added-in-version: "8.10"

    patch:
      x-eventually-consistent: false
      tags:
        - Agent instance
      operationId: updateAgentInstance
      security:
        - bearerAuth: []
        - basicAuth: []
      summary: Update agent instance
      description: |
        Updates the mutable fields of an agent instance: status, metric counters, and
        tools. Metric values are treated as deltas and applied immediately to the
        aggregate counters. Tool updates replace the existing tool list.
      parameters:
        - name: agentInstanceKey
          in: path
          required: true
          description: The key of the agent instance to update.
          schema:
            $ref: 'keys.yaml#/components/schemas/AgentInstanceKey'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AgentInstanceUpdateRequest'
      responses:
        "200":
          description: The agent instance was updated successfully.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AgentInstanceUpdateResult'
        "400":
          $ref: 'common-responses.yaml#/components/responses/InvalidData'
        "401":
          $ref: 'common-responses.yaml#/components/responses/Unauthorized'
        "403":
          $ref: 'common-responses.yaml#/components/responses/Forbidden'
        "404":
          description: |
            The agent instance with the given key was not found.
            More details are provided in the response body.
          content:
            application/problem+json:
              schema:
                $ref: 'problem-detail.yaml#/components/schemas/ProblemDetail'
        "500":
          $ref: 'common-responses.yaml#/components/responses/InternalServerError'
      x-added-in-version: "8.10"

  /agent-instances/search:
    post:
      x-eventually-consistent: true
      tags:
        - Agent instance
      operationId: searchAgentInstances
      security:
        - bearerAuth: []
        - basicAuth: []
      summary: Search agent instances
      description: Search for agent instances based on given criteria.
      requestBody:
        required: false
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AgentInstanceSearchQuery'
      responses:
        "200":
          description: The agent instance search result.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AgentInstanceSearchQueryResult'
        "400":
          $ref: 'common-responses.yaml#/components/responses/InvalidData'
        "401":
          $ref: 'common-responses.yaml#/components/responses/Unauthorized'
        "403":
          $ref: 'common-responses.yaml#/components/responses/Forbidden'
        "500":
          $ref: 'common-responses.yaml#/components/responses/InternalServerError'
      x-added-in-version: "8.10"

  /agent-instances/{agentInstanceKey}/history/search:
    post:
      x-eventually-consistent: true
      tags:
        - Agent instance
      operationId: searchAgentInstanceHistory
      security:
        - bearerAuth: []
        - basicAuth: []
      summary: Search agent instance history
      description: |
        Search the recorded history turns of a single agent instance. By default only
        turns whose commit status is COMMITTED are returned; pass a commitStatus filter to
        include PENDING or DISCARDED turns.
      parameters:
        - name: agentInstanceKey
          in: path
          required: true
          description: The key of the agent instance whose history to search.
          schema:
            $ref: 'keys.yaml#/components/schemas/AgentInstanceKey'
      requestBody:
        required: false
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AgentInstanceHistorySearchQuery'
      responses:
        "200":
          description: The agent instance history search result.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AgentInstanceHistorySearchQueryResult'
        "400":
          $ref: 'common-responses.yaml#/components/responses/InvalidData'
        "401":
          $ref: 'common-responses.yaml#/components/responses/Unauthorized'
        "403":
          $ref: 'common-responses.yaml#/components/responses/Forbidden'
        "500":
          $ref: 'common-responses.yaml#/components/responses/InternalServerError'
      x-added-in-version: "8.10"

## Schema definitions

components:
  schemas:
    AgentInstanceSearchQuerySortRequest:
      type: object
      required:
        - field
      properties:
        field:
          description: The field to sort by.
          type: string
          enum:
            - agentInstanceKey
            - agentDefinitionKey
            - status
            - elementId
            - processInstanceKey
            - rootProcessInstanceKey
            - processDefinitionKey
            - tenantId
            - creationDate
            - lastUpdatedDate
            - completionDate
        order:
          $ref: 'search-models.yaml#/components/schemas/SortOrderEnum'

    AgentInstanceSearchQuery:
      description: Agent instance search request.
      type: object
      additionalProperties: false
      allOf:
        - $ref: 'search-models.yaml#/components/schemas/SearchQueryRequest'
      properties:
        sort:
          description: Sort field criteria.
          type: array
          items:
            $ref: '#/components/schemas/AgentInstanceSearchQuerySortRequest'
        filter:
          description: The agent instance search filters.
          allOf:
            - $ref: '#/components/schemas/AgentInstanceFilter'

    AgentInstanceFilter:
      description: Agent instance search filter.
      type: object
      properties:
        agentInstanceKey:
          description: The unique key of the agent instance.
          allOf:
            - $ref: 'keys.yaml#/components/schemas/AgentInstanceKeyFilterProperty'
        status:
          description: The current status of the agent instance.
          allOf:
            - $ref: '#/components/schemas/AgentInstanceStatusFilterProperty'
        elementId:
          description: The BPMN element ID of the agent task.
          allOf:
            - $ref: 'identifiers.yaml#/components/schemas/ElementIdFilterProperty'
        processInstanceKey:
          description: The key of the process instance that owns this agent instance.
          allOf:
            - $ref: 'keys.yaml#/components/schemas/ProcessInstanceKeyFilterProperty'
        processDefinitionKey:
          description: The key of the process definition associated with this agent instance.
          allOf:
            - $ref: 'keys.yaml#/components/schemas/ProcessDefinitionKeyFilterProperty'
        tenantId:
          description: The tenant ID of the agent instance.
          allOf:
            - $ref: 'filters.yaml#/components/schemas/StringFilterProperty'
        creationDate:
          description: The creation date of the agent instance.
          allOf:
            - $ref: 'filters.yaml#/components/schemas/DateTimeFilterProperty'
        lastUpdatedDate:
          description: The date the agent instance was last updated.
          allOf:
            - $ref: 'filters.yaml#/components/schemas/DateTimeFilterProperty'
        completionDate:
          description: The completion date of the agent instance.
          allOf:
            - $ref: 'filters.yaml#/components/schemas/DateTimeFilterProperty'
        elementInstanceKeys:
            description: |-
              The keys of element instances associated with this agent instance.
              If multiple keys are provided, the filter matches agent instances associated with all of the provided keys at the same time.
            type: array
            items:
              allOf:
                - $ref: 'keys.yaml#/components/schemas/ElementInstanceKeyFilterProperty'
        processDefinitionId:
          description: The BPMN process ID of the process definition associated with this agent instance.
          allOf:
            - $ref: 'filters.yaml#/components/schemas/StringFilterProperty'
        processDefinitionVersion:
          description: The version of the process definition associated with this agent instance.
          allOf:
            - $ref: 'filters.yaml#/components/schemas/IntegerFilterProperty'
        processDefinitionVersionTag:
          description: The version tag of the process definition associated with this agent instance.
          allOf:
            - $ref: 'filters.yaml#/components/schemas/StringFilterProperty'

    AgentInstanceSearchQueryResult:
      description: Agent instance search response.
      type: object
      required:
        - items
      allOf:
        - $ref: 'search-models.yaml#/components/schemas/SearchQueryResponse'
      properties:
        items:
          description: The matching agent instances.
          type: array
          items:
            $ref: '#/components/schemas/AgentInstanceResult'

    AgentInstanceResult:
      type: object
      required:
        - agentInstanceKey
        - status
        - definition
        - metrics
        - limits
        - tools
        - elementId
        - processInstanceKey
        - rootProcessInstanceKey
        - processDefinitionKey
        - processDefinitionId
        - processDefinitionVersion
        - processDefinitionVersionTag
        - tenantId
        - creationDate
        - lastUpdatedDate
        - completionDate
        - elementInstanceKeys
      properties:
        agentInstanceKey:
          description: The unique key for this agent instance.
          allOf:
            - $ref: 'keys.yaml#/components/schemas/AgentInstanceKey'
        status:
          $ref: '#/components/schemas/AgentInstanceStatusEnum'
        definition:
          description: The static definition of the agent, including model, provider, and system prompt.
          allOf:
            - $ref: '#/components/schemas/AgentInstanceDefinition'
        metrics:
          description: Aggregated metrics across all iterations of this agent instance.
          allOf:
            - $ref: '#/components/schemas/AgentInstanceMetrics'
        limits:
          description: The configured limits for this agent instance, set once at creation.
          allOf:
            - $ref: '#/components/schemas/AgentInstanceLimits'
        tools:
          description: The tools available to the agent.
          type: array
          items:
            $ref: '#/components/schemas/AgentTool'
        elementId:
          description: The BPMN element ID of the ad-hoc sub-process or AI agent task that owns this agent instance.
          allOf:
            - $ref: 'identifiers.yaml#/components/schemas/ElementId'
        processInstanceKey:
          description: The key of the process instance that owns this agent instance.
          allOf:
            - $ref: 'keys.yaml#/components/schemas/ProcessInstanceKey'
        rootProcessInstanceKey:
          description: |
            The key of the root process instance. The root process instance is the top-level
            ancestor in the process instance hierarchy.
          allOf:
            - $ref: 'keys.yaml#/components/schemas/ProcessInstanceKey'
        processDefinitionKey:
          description: The key of the process definition associated with this agent instance.
          allOf:
            - $ref: 'keys.yaml#/components/schemas/ProcessDefinitionKey'
        processDefinitionId:
          description: The BPMN process ID of the process definition associated with this agent instance.
          allOf:
            - $ref: 'identifiers.yaml#/components/schemas/ProcessDefinitionId'
        processDefinitionVersion:
          type: integer
          format: int32
          description: The version of the process definition associated with this agent instance.
        processDefinitionVersionTag:
          type: string
          nullable: true
          description: The version tag of the process definition associated with this agent instance.
        tenantId:
          description: The tenant ID of this agent instance.
          allOf:
            - $ref: 'identifiers.yaml#/components/schemas/TenantId'
        creationDate:
          type: string
          format: date-time
          description: The date when this agent instance was created.
        lastUpdatedDate:
          type: string
          format: date-time
          description: The date when this agent instance was last updated.
        completionDate:
          type: string
          format: date-time
          nullable: true
          description: The date when this agent instance completed. Null while the agent is still running.
        elementInstanceKeys:
          description: The keys of all element instances associated with this agent instance.
          type: array
          items:
            allOf:
                - $ref: 'keys.yaml#/components/schemas/ElementInstanceKey'

    AgentInstanceDefinition:
      description: The static definition of an agent instance, set once at creation.
      type: object
      required:
        - model
        - provider
        - systemPrompt
      properties:
        model:
          type: string
          description: The LLM model identifier (for example, gpt-4o).
        provider:
          type: string
          description: The LLM provider (for example, openai or anthropic).
        systemPrompt:
          type: string
          description: The system prompt configured for this agent instance.

    AgentTool:
      description: A tool available to the agent.
      type: object
      required:
        - name
        - description
        - elementId
      properties:
        name:
          type: string
          description: The tool name as visible to the LLM.
        description:
          type: string
          nullable: true
          description: A human-readable description of the tool.
        elementId:
          type: string
          nullable: true
          description: The BPMN element ID of the tool element within the ad-hoc sub-process.

    AgentInstanceMetrics:
      description: Aggregated metrics for an agent instance across all model calls.
      type: object
      required:
        - inputTokens
        - outputTokens
        - modelCalls
        - toolCalls
      properties:
        inputTokens:
          type: integer
          format: int64
          description: Total input tokens consumed across all model calls.
        outputTokens:
          type: integer
          format: int64
          description: Total output tokens produced across all model calls.
        modelCalls:
          type: integer
          format: int32
          description: Total number of LLM calls made.
        toolCalls:
          type: integer
          format: int32
          description: Total number of tool calls made.

    AgentInstanceLimits:
      description: The configured limits for an agent instance, set once at creation.
      type: object
      required:
        - maxModelCalls
        - maxTokens
        - maxToolCalls
      properties:
        maxModelCalls:
          type: integer
          format: int32
          description: Maximum LLM calls allowed. -1 if no limit is configured.
        maxToolCalls:
          type: integer
          format: int32
          description: Maximum tool calls allowed. -1 if no limit is configured.
        maxTokens:
          type: integer
          format: int64
          description: Maximum total tokens allowed. -1 if no limit is configured.

    AgentInstanceStatusEnum:
      description: The current status of an agent instance.
      type: string
      enum:
        - UNKNOWN
        - COMPLETED
        - IDLE
        - INITIALIZING
        - THINKING
        - TOOL_CALLING
        - TOOL_DISCOVERY

    AgentInstanceUpdateStatusEnum:
      description: |
        The status values that can be set on an agent instance via an update request.
      type: string
      enum:
        - IDLE
        - THINKING
        - TOOL_CALLING
        - TOOL_DISCOVERY

    AgentInstanceCreationRequest:
      description: Request to create a new agent instance.
      type: object
      required:
        - elementInstanceKey
        - definition
      properties:
        elementInstanceKey:
          description: |
            The key of the AHSP or AI Agent Task element instance.
            The engine uses this key to infer processInstanceKey, elementId,
            processDefinitionKey, and tenantId.
          allOf:
            - $ref: 'keys.yaml#/components/schemas/ElementInstanceKey'
        definition:
          description: Static definition set once at creation.
          allOf:
            - $ref: '#/components/schemas/AgentInstanceDefinition'
        limits:
          description: |
            Limits for the agent execution. When omitted, all limits default to -1
            (no limit).
          allOf:
            - $ref: '#/components/schemas/AgentInstanceLimits'

    AgentInstanceCreationResult:
      description: Response returned after successfully creating an agent instance.
      type: object
      x-semantic-provider:
        - agentInstanceKey
      required:
        - agentInstanceKey
      properties:
        agentInstanceKey:
          description: The system-generated key for the created agent instance.
          allOf:
            - $ref: 'keys.yaml#/components/schemas/AgentInstanceKey'

    AgentInstanceMetricsDelta:
      description: |
        Metric increments to apply to the agent instance aggregate counters. The engine
        accumulates these deltas into running totals on each UPDATED event. All fields
        are optional; omit a field to leave the corresponding counter unchanged.
      type: object
      properties:
        inputTokens:
          type: integer
          format: int64
          minimum: 0
          description: Increment to apply to the total input token counter.
        outputTokens:
          type: integer
          format: int64
          minimum: 0
          description: Increment to apply to the total output token counter.
        modelCalls:
          type: integer
          format: int32
          minimum: 0
          description: Increment to apply to the total model call counter.
        toolCalls:
          type: integer
          format: int32
          minimum: 0
          description: Increment to apply to the total tool call counter.

    AgentInstanceUpdateRequest:
      description: |
        Request to update the mutable state of an agent instance: advance its status and
        append a batch of history items to its conversation history. Each created history
        item is echoed back in the response's createdHistory, positionally correlated.
      type: object
      required:
        - elementInstanceKey
      properties:
        elementInstanceKey:
          nullable: false
          description: |
            The key of the currently-active element instance for this agent instance.
            Used for ownership/equality validation against the stored agent instance
            and, when the supplied key differs from the previous association (re-entry
            of an ad-hoc sub-process or AI Agent task), appended to elementInstanceKeys
            with the reverse link updated on the supplied element instance.
          allOf:
            - $ref: 'keys.yaml#/components/schemas/ElementInstanceKey'
        status:
          description: The new status of the agent instance.
          allOf:
            - $ref: '#/components/schemas/AgentInstanceUpdateStatusEnum'
        jobKey:
          description: |
            The key of the job activation during which this update is being made. Attributed
            to each appended history item so a later retry can supersede in-flight items.
          allOf:
            - $ref: 'keys.yaml#/components/schemas/JobKey'
        jobLease:
          description: |
            Lease token received from the job activation response, disambiguating this
            activation from any other of the same job. It is the activation's lease
            deadline serialized as a decimal string (an unsigned 64-bit integer), so
            the server rejects any non-numeric value with 400.
          type: string
          pattern: ^[0-9]+$
          minLength: 1
          maxLength: 20
          example: "1717000000000"
        metrics:
          description: Metric increments to apply to the aggregate counters.
          allOf:
            - $ref: '#/components/schemas/AgentInstanceMetricsDelta'
        tools:
          description: |
            The complete list of tools available to the agent, replacing any previously
            stored tools. When provided, the engine replaces the existing tool list with
            this value.
          nullable: true
          type: array
          items:
            $ref: '#/components/schemas/AgentTool'
        history:
          description: |
            A batch of history items to append to the agent instance's conversation history,
            in request order. Each created item is echoed back in the response's createdHistory,
            positionally correlated.
          nullable: true
          type: array
          items:
            $ref: '#/components/schemas/AgentInstanceHistoryItem'

    AgentInstanceUpdateResult:
      description: Response returned after successfully updating an agent instance.
      type: object
      required:
        - createdHistory
      properties:
        createdHistory:
          description: |
            One entry per history item submitted in the request, in request order.
            Empty when no history items were submitted.
          type: array
          items:
            $ref: '#/components/schemas/AgentInstanceCreatedHistoryItem'

    AgentInstanceCreatedHistoryItem:
      description: The outcome of appending a single history item from an update request's history batch.
      type: object
      required:
        - historyItemId
        - historyItemKey
        - isDuplicate
      properties:
        historyItemId:
          description: The historyItemId of the corresponding request item, echoed back for correlation.
          type: string
        historyItemKey:
          description: |
            The system-generated key for the history item. When isDuplicate is true, this is
            the key of the original entry, not a new one.
          allOf:
            - $ref: 'keys.yaml#/components/schemas/AgentHistoryKey'
        isDuplicate:
          description: |
            True if this item had already been recorded and no new AGENT_HISTORY event was
            created for it; false if a new event was created.
          type: boolean

    ## Filters

    AgentInstanceStatusFilterProperty:
      description: AgentInstanceStatusEnum property with full advanced search capabilities.
      oneOf:
        - type: string
          title: Exact match
          description: Matches the value exactly.
          allOf:
            - $ref: '#/components/schemas/AgentInstanceStatusEnum'
        - $ref: '#/components/schemas/AdvancedAgentInstanceStatusFilter'

    AdvancedAgentInstanceStatusFilter:
      title: Advanced filter
      description: Advanced AgentInstanceStatusEnum filter.
      type: object
      properties:
        $eq:
          description: Checks for equality with the provided value.
          allOf:
            - $ref: '#/components/schemas/AgentInstanceStatusEnum'
        $neq:
          description: Checks for inequality with the provided value.
          allOf:
            - $ref: '#/components/schemas/AgentInstanceStatusEnum'
        $exists:
          description: Checks if the current property exists.
          type: boolean
        $in:
          description: Checks if the property matches any of the provided values.
          type: array
          items:
            $ref: '#/components/schemas/AgentInstanceStatusEnum'
        $like:
          $ref: 'filters.yaml#/components/schemas/LikeFilter'


    ## Agent instance history

    AgentInstanceHistorySearchQuery:
      description: Agent instance history search request.
      type: object
      additionalProperties: false
      allOf:
        - $ref: 'search-models.yaml#/components/schemas/SearchQueryRequest'
      properties:
        sort:
          description: Sort field criteria.
          type: array
          items:
            $ref: '#/components/schemas/AgentInstanceHistorySearchQuerySortRequest'
        filter:
          description: The history item search filters.
          allOf:
            - $ref: '#/components/schemas/AgentInstanceHistoryFilter'

    AgentInstanceHistorySearchQuerySortRequest:
      type: object
      required:
        - field
      properties:
        field:
          description: The field to sort by.
          type: string
          enum:
            - producedAt
            - historyItemKey
            - loopIteration
        order:
          $ref: 'search-models.yaml#/components/schemas/SortOrderEnum'

    AgentInstanceHistoryFilter:
      description: |
        Agent instance history item search filter. When commitStatus is omitted, only
        COMMITTED history items are returned.
      type: object
      properties:
        role:
          description: The role of the history item.
          allOf:
            - $ref: '#/components/schemas/AgentInstanceHistoryRoleFilterProperty'
        loopIteration:
          description: Filter by loop iteration number.
          allOf:
            - $ref: 'filters.yaml#/components/schemas/IntegerFilterProperty'
        commitStatus:
          description: |
            The commit status of the history item. Defaults to COMMITTED only.
            Include PENDING or DISCARDED explicitly to debug in-flight or failed activations.
          allOf:
            - $ref: '#/components/schemas/AgentInstanceHistoryCommitStatusFilterProperty'
        producedAt:
          description: The timestamp when the history item was produced.
          allOf:
            - $ref: 'filters.yaml#/components/schemas/DateTimeFilterProperty'

    AgentInstanceHistorySearchQueryResult:
      description: Agent instance history search response.
      type: object
      required:
        - items
      allOf:
        - $ref: 'search-models.yaml#/components/schemas/SearchQueryResponse'
      properties:
        items:
          description: The matching history items.
          type: array
          items:
            $ref: '#/components/schemas/AgentInstanceHistoryItemResult'

    AgentInstanceHistoryItem:
      description: |
        A single history item to append to the agent instance's conversation history,
        submitted as part of the batch on an agent instance update request.
      type: object
      required:
        - historyItemId
        - loopIteration
        - role
        - content
        - producedAt
      properties:
        historyItemId:
          description: |
            Caller-assigned identifier used to detect and dedupe retries of the same item.
            Must be non-blank.
          type: string
        loopIteration:
          type: integer
          format: int32
          description: The loop iteration this item belongs to.
        role:
          description: The role of this history item in the conversation.
          allOf:
            - $ref: '#/components/schemas/AgentInstanceHistoryRoleEnum'
        content:
          description: The content blocks of this history item.
          type: array
          items:
            $ref: '#/components/schemas/AgentInstanceMessageContent'
        toolCalls:
          description: Tool calls associated with this history item.
          type: array
          nullable: true
          items:
            $ref: '#/components/schemas/AgentInstanceToolCall'
        metrics:
          description: Per-call token and latency metrics. Present on ASSISTANT items only.
          nullable: true
          allOf:
            - $ref: '#/components/schemas/AgentInstanceHistoryItemMetrics'
        producedAt:
          type: string
          format: date-time
          description: The agent-side timestamp of when this message was produced.
        tools:
          description: The complete list of tools available to the agent as of this entry. CONFIGURATION items only.
          type: array
          nullable: true
          items:
            $ref: '#/components/schemas/AgentTool'
        model:
          type: string
          nullable: true
          description: The LLM model identifier as of this entry. CONFIGURATION items only.
        provider:
          type: string
          nullable: true
          description: The LLM provider as of this entry. CONFIGURATION items only.
        limits:
          description: The operational limits as of this entry. CONFIGURATION items only.
          allOf:
            - $ref: '#/components/schemas/AgentInstanceLimits'
        systemPrompt:
          type: string
          nullable: true
          description: The system prompt as of this entry. CONFIGURATION items only.

    AgentInstanceHistoryItemResult:
      description: A single conversation history item belonging to an agent instance.
      type: object
      required:
        - historyItemKey
        - historyItemId
        - agentInstanceKey
        - elementInstanceKey
        - processInstanceKey
        - rootProcessInstanceKey
        - processDefinitionKey
        - processDefinitionId
        - tenantId
        - jobKey
        - loopIteration
        - role
        - content
        - toolCalls
        - metrics
        - commitStatus
        - producedAt
        - tools
        - model
        - provider
        - isDuplicate
      properties:
        historyItemKey:
          description: The unique key for this history item. Stable and sortable by creation order.
          allOf:
            - $ref: 'keys.yaml#/components/schemas/AgentHistoryKey'
        historyItemId:
          type: string
          description: The client-supplied identifier this item was created with. Empty when none.
        agentInstanceKey:
          description: The key of the agent instance this item belongs to.
          allOf:
            - $ref: 'keys.yaml#/components/schemas/AgentInstanceKey'
        elementInstanceKey:
          description: The element instance under which this item was produced.
          allOf:
            - $ref: 'keys.yaml#/components/schemas/ElementInstanceKey'
        processInstanceKey:
          description: The owning process instance key.
          allOf:
            - $ref: 'keys.yaml#/components/schemas/ProcessInstanceKey'
        rootProcessInstanceKey:
          description: The root process instance key.
          allOf:
            - $ref: 'keys.yaml#/components/schemas/ProcessInstanceKey'
        processDefinitionKey:
          description: The process definition key.
          allOf:
            - $ref: 'keys.yaml#/components/schemas/ProcessDefinitionKey'
        processDefinitionId:
          description: The BPMN process ID of the process definition.
          allOf:
            - $ref: 'identifiers.yaml#/components/schemas/ProcessDefinitionId'
        tenantId:
          description: The tenant ID of this history item.
          allOf:
            - $ref: 'identifiers.yaml#/components/schemas/TenantId'
        jobKey:
          description: The key of the job activation during which this item was produced.
          allOf:
            - $ref: 'keys.yaml#/components/schemas/JobKey'
        loopIteration:
          type: integer
          format: int32
          description: The loop iteration this item belongs to.
        role:
          description: The role of this history item in the conversation.
          allOf:
            - $ref: '#/components/schemas/AgentInstanceHistoryRoleEnum'
        content:
          description: The content blocks of this history item.
          type: array
          items:
            $ref: '#/components/schemas/AgentInstanceMessageContent'
        toolCalls:
          description: Tool calls for this item.
          type: array
          items:
            $ref: '#/components/schemas/AgentInstanceToolCall'
        metrics:
          description: Per-call token and latency metrics. Null when metrics were not provided at creation time.
          nullable: true
          allOf:
            - $ref: '#/components/schemas/AgentInstanceHistoryItemMetrics'
        commitStatus:
          description: The commit status of this history item.
          allOf:
            - $ref: '#/components/schemas/AgentInstanceHistoryCommitStatusEnum'
        producedAt:
          type: string
          format: date-time
          description: The agent-side timestamp of when this message was produced.
        tools:
          description: The complete list of tools available to the agent as of this entry.
          type: array
          items:
            $ref: '#/components/schemas/AgentTool'
        model:
          type: string
          nullable: true
          description: The LLM model identifier as of this entry.
        provider:
          type: string
          nullable: true
          description: The LLM provider as of this entry.
        isDuplicate:
          type: boolean
          description: Whether this item was recorded as a duplicate of an earlier one.

    AgentInstanceHistoryRoleEnum:
      description: The role of a history item in the agent conversation.
      type: string
      enum:
        - USER
        - ASSISTANT
        - TOOL_RESULT
        - CONFIGURATION

    AgentInstanceHistoryCommitStatusEnum:
      description: The commit status of a history item.
      type: string
      enum:
        - COMMITTED
        - PENDING
        - DISCARDED

    AgentInstanceMessageContent:
      description: |
        A single content block within a history item. The `contentType` discriminator selects which
        payload field carries the content: TEXT → `text`, DOCUMENT → `documentReference`,
        OBJECT → `object`.
      type: object
      required:
        - contentType
      properties:
        contentType:
          description: Which payload field carries this block's content.
          allOf:
            - $ref: '#/components/schemas/AgentInstanceContentTypeEnum'
        text:
          type: string
          nullable: true
          description: The plain-text payload. Present when contentType is TEXT.
        documentReference:
          type: string
          nullable: true
          description: An opaque reference to a stored document. Present when contentType is DOCUMENT.
        object:
          nullable: true
          description: Arbitrary structured content (any valid JSON value). Present when contentType is OBJECT.

    AgentInstanceContentTypeEnum:
      description: The type of a single content block.
      type: string
      enum:
        - TEXT
        - DOCUMENT
        - OBJECT

    AgentInstanceToolCall:
      description: A tool call associated with a history item.
      type: object
      required:
        - toolCallId
        - toolName
        - elementId
        - arguments
      properties:
        toolCallId:
          type: string
          description: The LLM-assigned tool call ID.
        toolName:
          type: string
          description: The LLM-visible tool name.
        elementId:
          type: string
          nullable: true
          description: The BPMN element ID handling this tool.
        arguments:
          type: object
          nullable: true
          additionalProperties: true
          description: The tool call arguments as provided by the LLM.

    AgentInstanceHistoryItemMetrics:
      description: Per-call token and latency metrics for an ASSISTANT history item.
      type: object
      required:
        - inputTokens
        - outputTokens
        - durationMs
      properties:
        inputTokens:
          type: integer
          format: int64
          nullable: true
          description: Input tokens consumed by this LLM call.
        outputTokens:
          type: integer
          format: int64
          nullable: true
          description: Output tokens produced by this LLM call.
        durationMs:
          type: integer
          format: int64
          nullable: true
          description: Wall-clock duration of the LLM call in milliseconds.

    ## History filters

    AgentInstanceHistoryRoleFilterProperty:
      description: AgentInstanceHistoryRoleEnum property with full advanced search capabilities.
      oneOf:
        - type: string
          title: Exact match
          description: Matches the value exactly.
          allOf:
            - $ref: '#/components/schemas/AgentInstanceHistoryRoleEnum'
        - $ref: '#/components/schemas/AdvancedAgentInstanceHistoryRoleFilter'

    AdvancedAgentInstanceHistoryRoleFilter:
      title: Advanced filter
      description: Advanced AgentInstanceHistoryRoleEnum filter.
      type: object
      properties:
        $eq:
          allOf:
            - $ref: '#/components/schemas/AgentInstanceHistoryRoleEnum'
          description: Checks for equality with the provided value.
        $neq:
          allOf:
            - $ref: '#/components/schemas/AgentInstanceHistoryRoleEnum'
          description: Checks for inequality with the provided value.
        $exists:
          type: boolean
          description: Checks if the current property exists.
        $in:
          type: array
          items:
            $ref: '#/components/schemas/AgentInstanceHistoryRoleEnum'
          description: Checks if the property matches any of the provided values.

    AgentInstanceHistoryCommitStatusFilterProperty:
      description: AgentInstanceHistoryCommitStatusEnum property with full advanced search capabilities.
      oneOf:
        - type: string
          title: Exact match
          description: Matches the value exactly.
          allOf:
            - $ref: '#/components/schemas/AgentInstanceHistoryCommitStatusEnum'
        - $ref: '#/components/schemas/AdvancedAgentInstanceHistoryCommitStatusFilter'

    AdvancedAgentInstanceHistoryCommitStatusFilter:
      title: Advanced filter
      description: Advanced AgentInstanceHistoryCommitStatusEnum filter.
      type: object
      properties:
        $eq:
          allOf:
            - $ref: '#/components/schemas/AgentInstanceHistoryCommitStatusEnum'
          description: Checks for equality with the provided value.
        $neq:
          allOf:
            - $ref: '#/components/schemas/AgentInstanceHistoryCommitStatusEnum'
          description: Checks for inequality with the provided value.
        $exists:
          type: boolean
          description: Checks if the current property exists.
        $in:
          type: array
          items:
            $ref: '#/components/schemas/AgentInstanceHistoryCommitStatusEnum'
          description: Checks if the property matches any of the provided values.
